# 更新日志

## [0.5.0] - 2026-06-07

### 新增
- **工作日历**：全新的日历 Tab，支持月度视图、日期类型标记（工作日/休息日/节假日/加班/请假）
- 法定节假日数据对接 timor.tech API，自动识别国家法定节假日和调休上班日
- 支持跨年节假日加载（如春节跨 1-2 月）
- 节假日数据缓存机制，支持手动刷新
- 日历右侧常驻面板：未选日期时显示当月统计（工作日/休息日/节假日数量），选中日期时显示日期详情和日程管理
- 可点击标签直接标记日期类型，无需通过表单
- 月度日程管理：添加、删除日程，支持标题和备注
- **文档审查重设计**：审查流程改为模板→文件级联选择
- 先选择审查模板，自动筛选该项目下匹配的文件列表（按 templateId 或 category 匹配）
- 文件列表以卡片形式展示：文件名、版本信息、进度条
- 审查时自动关联文件版本，无需手动选择
- 模板与文件类型不匹配时显示警告提示
- **版本对比移至审查页**：版本对比从独立 Tab 合并到审查页下方垂直布局
- 支持 AI 分析对比差异，生成变更分析报告
- **AI 模型独立测试**：每个 AI 模型支持单独测试连通性
- 多模型调用模式：单模型调用和多模型并行输出
- **模板结构编辑器重构**：改为可视化章节列表，左右分栏布局
- 所有阶段（包括系统阶段）统一可编辑和删除
- 自定义阶段支持名称、识别关键词和颜色配置
- **文件拖拽**：文件管理器支持原生拖拽操作
- 拖出文件：通过 `webContents.startDrag` 实现原生文件拖拽，外部应用（微信、Word 等）可接收实际文件
- 拖入文件到项目卡片：从外部直接拖入文件到项目
- 拖入文件到子文件夹：拖入文件到特定子目录
- **资源管理器风格文件操作**：文件管理器交互改为 Windows 资源管理器风格
- 单击选中文件，蓝色高亮
- 慢双击（间隔 < 500ms）或 F2 进入重命名模式
- Delete 键删除选中文件，Escape 取消重命名
- 点击空白处取消选中
- **模板内容保留**：模板导入和文件创建保留完整内容
- 导入模板时保留每个章节之间的完整原始内容（不再截断到 1200 字符）
- 从模板创建 .docx 文件时，生成的文档包含完整的章节标题和正文内容框架

### 改进
- AI 响应解析支持 DeepSeek 等非标准格式
- AI 调用添加详细日志，便于排查连接失败问题
- 滚动条样式优化：胶囊样式、渐显渐隐动画、自动隐藏
- 全局禁止横向滚动
- 折叠面板展开/收起添加线性过渡动画
- 文件 Tab 和进度 Tab 改用折叠面板展开，不再弹出抽屉窗口
- 进度 Tab 按阶段分类显示所有文档进度
- 侧边栏导航更新：新增计划、日历、团队、报告入口

### 修复
- 统一项目进度计算函数，全局使用 getProjectProgress
- 彩条末端时间修正：导入项目使用文件最后修改时间
- 导出 ZIP 使用项目名称命名，进度条动态计算

## [0.4.0] - 2026-06-01

### 新增
- **自定义项目阶段**：在模板管理页面新增阶段管理功能，支持自定义阶段名称、识别关键词和颜色
- 默认4个系统阶段（提案、指南编写、可研、其他）不可删除，可新增自定义阶段
- 自定义阶段通过文件名关键词自动识别，与系统阶段统一参与甘特图、统计、计划等所有功能
- 阶段颜色自定义，颜色选择器自动排除已使用颜色

### 修复
- 彩条末端时间：导入项目使用文件最后修改时间，工作区内项目使用当前时间

## [0.3.0] - 2026-06-01

### 新增
- **项目导入支持 ZIP 压缩包**：导入项目时可选择从文件夹导入或从 ZIP 压缩包导入
- **项目导出为 ZIP**：每个项目卡片新增「导出」按钮，可将项目文件夹和元数据打包为 ZIP
- ZIP 导入时自动识别 `project.json` 元数据文件，恢复项目记录和文档关联
- 无元数据的 ZIP 按文件夹名创建项目，自动匹配模板
- 导入项目自动匹配模板：通过文件名关键字识别阶段并关联对应模板
- 侧边栏概览Tab重构：百分比统计、下一步建议、跨项目阶段汇总
- 侧边栏文件Tab：支持滚动、按模板自动分组、填满窗口高度
- 文件浏览器：文件类型筛选、概览卡片交互优化、返回按钮

### 修复
- 甘特图滚动区域高度按前3个项目实际内容计算，超出才显示滚动条
- 网格线高度改为实际内容高度，消除滚动空白区域
- 逾期/即将逾期判断逻辑：无具体时间按天判断，有具体时间按24小时判断
- 截止时间规范化为 midnight，避免时区导致的判断错误
- 全局同步逾期/即将逾期判断逻辑（甘特图、计划侧边栏、项目表格、统计卡片）
- 文件类型统计将非标准类型归入"其他"，筛选时排除文件夹
- 侧边栏文件Tab填满窗口高度

## [0.2.0] - 2026-05-28

### 新增
- 甘特图每个阶段独立一行，支持垂直滚动
- 甘特图左侧分为项目名（一级）和阶段名（二级）两栏
- 项目按最近更新时间排序
- 逾期标志（红色三角）合并到彩条内部，与彩条颜色同步
- 彩条和标签圆点颜色同步：逾期红、即将逾期黄、正常阶段色
- 文件浏览器概览卡片：文件数量、总大小、文件夹数、各类型统计
- 统计卡片新增「即将逾期」

### 修复
- 甘特图滚动事件隔离，时间线滚动不影响主页面
- 彩条移除文字，避免文字溢出
- 逾期虚线框合并到彩条
- 项目名称居中显示

## [0.1.0] - 2026-05-25

### 初始版本
- 项目管理基础功能：创建、导入、删除项目
- 甘特图时间线可视化
- 模板管理：导入文档提取模板结构
- 文档审查：对比模板检查格式
- 版本管理：文档版本对比
- AI 辅助功能：摘要生成、审查建议
- 文件浏览器：查看项目文件夹内容

